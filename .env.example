# Supabase project settings -> API
# Both are safe in the browser: the anon key only grants what the RLS policies
# in supabase/migrations allow. The service_role key must NEVER go in here.
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-anon-key
